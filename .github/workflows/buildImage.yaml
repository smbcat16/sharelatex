name: Docker Image github CI

on:
  push:
  repository_dispatch:
  workflow_dispatch:
    inputs:
      buildBaseImage:
        description: 'Build base image'
        required: false
        default: true
        type: boolean
      buildImage:
        description: 'Build image'
        required: false
        default: true
        type: boolean

jobs:

  build:
    strategy:
      matrix:
        include:
          - runs-on: ubuntu-latest
            architecture: arm64
            scheme-full: false
    runs-on: ${{ matrix.runs-on }}

    steps:
      -
        name: Checkout Overleaf
        uses: actions/checkout@v4
        with:
          repository: "overleaf/overleaf"
          ref: "main"
          path: "${{ github.workspace }}"        
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      -
        name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Change scheme
        working-directory: ${{ github.workspace }}
        if: inputs.buildBaseImage && matrix.scheme-full
        run: sed -Ei 's/scheme-basic/scheme-full/' server-ce/Dockerfile-base
      -
        name: Build and push base image
        if: ${{ inputs.buildBaseImage }}
        uses: docker/build-push-action@v4
        with:
          platforms: linux/${{ matrix.architecture }}
          context: ${{ github.workspace }}
          file: server-ce/Dockerfile-base
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sharelatex-base:latest
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sharelatex-base:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sharelatex-base:buildcache,mode=max 
      -
        name: Build and push image
        if: ${{ inputs.buildImage }}
        uses: docker/build-push-action@v4
        with:
          platforms: linux/${{ matrix.architecture }}
          context: ${{ github.workspace }}
          #directory: server-ce
          file: server-ce/Dockerfile
          build-args: SHARELATEX_BASE_TAG=${{ secrets.DOCKERHUB_USERNAME }}/sharelatex-base
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/sharelatex:latest
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sharelatex:buildcache
          cache-to: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/sharelatex:buildcache,mode=max
